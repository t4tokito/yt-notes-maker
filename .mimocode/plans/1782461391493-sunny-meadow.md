# UI Polish Plan — Notes Maker App

## Goal
Full UI cleanup: remove dead code, fix theme colors, make every screen consistent with a minimal clean look.

---

## Step 1: Clean Dead Code
Remove files that are never used:
- Delete `app/modern-home.tsx`, `app/modern-chat.tsx`, `app/modern-create.tsx`, `app/modern-notes.tsx`, `app/modern-profile.tsx`
- Delete `lib/designSystem.ts`
- Delete `app/study-planner.tsx`
- Delete `global.css`, `tailwind.config.js`, `nativewind-env.d.ts`
- Remove `import "../global.css"` from `app/_layout.tsx`
- Remove `LinearGradient` unused import from `app/index.tsx`
- Remove double `GestureHandlerRootView` from individual screens (index, notes, create, flashcards, ai-tools, pdf-notes) — `_layout.tsx` already wraps everything

## Step 2: Expand Theme Colors
Update `lib/theme.tsx` to include all hardcoded colors:

```
onlineGreen: "#64C878"
errorRed: "#E85D5D"
successGreen: "#64C878"
toolTest: "#64C878"
toolPdf: "#E85D5D"
toolFlashcards: "#9B8A70"
toolExplain: "#E8A04A"
subadminGreen: "rgba(100, 200, 120, 0.15)"
subadminText: "#64C878"
```

Then replace all hardcoded hex values across the app with `colors.xxx`.

## Step 3: Fix ProfileMenu + BottomNav
- `components/ProfileMenu.tsx` — replace hardcoded `"#fff"` and `"#DDAAAA"` with theme colors
- `components/BottomNav.tsx` — fix active tab: instead of `opacity: 0.5`, use `opacity: 1` with accent-colored icon and a small dot indicator below

## Step 4: Standardize Screens — Titles
All screen titles should be: `fontSize: 18, fontWeight: "700"` — consistent across:
- app/flashcards.tsx (currently custom back + title)
- app/new.tsx (currently fontSize: 26, fontWeight: "800")
- app/ai-tools.tsx
- app/pdf-notes.tsx
- app/explain.tsx

## Step 5: Standardize CTA Buttons
All primary action buttons: `height: 52, borderRadius: 14, fontWeight: "700", fontSize: 15`
- app/new.tsx (currently height: 56, borderRadius: 16)
- app/pdf-notes.tsx (currently height: 50)
- app/flashcards.tsx (currently height: 48, borderRadius: 14)
- app/explain.tsx (currently height: 52)
- app/login.tsx (currently height: 48, borderRadius: 12)

## Step 6: Standardize Card Border Radius
- Cards: `borderRadius: 16` everywhere (currently 4-24px range)
- Inputs: `borderRadius: 12` everywhere (currently 10-14px range)
- Modals: `borderRadius: 16` (currently 14)

Apply to:
- app/profile.tsx (grid cards currently borderRadius: 4)
- app/notes.tsx (list cards currently borderRadius: 12)
- app/create.tsx ToolCard (currently borderRadius: 24)
- All modal containers

## Step 7: Standardize Empty States
Create a consistent empty state pattern:
- Icon size: 48, color: muted
- Text: fontSize: 14, color: muted, marginTop: 10
- Container: padding: 32, borderRadius: 16, backgroundColor: card, borderWidth: 1, borderColor: border

Apply to: index.tsx, notes.tsx, friends.tsx, chat.tsx, chat/[id].tsx, group/[id].tsx

## Step 8: Standardize Back Navigation
All sub-screens use same pattern: 40x40 rounded square button with arrow-back icon
- app/flashcards.tsx (currently text "Back" link)
- app/new.tsx (currently text "Back" link)
- app/ai-tools.tsx (currently text "Back" link)
- app/pdf-notes.tsx (currently text "Back" link)
- app/explain.tsx (already has this pattern)

## Step 9: Fix Profile Screen
- Use `Header` component instead of manual title positioning
- Fix grid card `borderRadius: 4` to `16`
- Fix grid card `gap: 2` to `10`

## Step 10: Clean Up Unused Routes
- Remove `study-planner` from `_layout.tsx` Stack if registered
- Verify all registered routes have corresponding files

---

## Verification
After each step, check:
1. No broken imports (grep for deleted file names)
2. App loads without crashes on Expo Go
3. Visual consistency across all main screens (Home, Notes, Create, Friends, Chat, Profile)
